# Themes

`cc-dark` and `cc-light` are vendored from the npm package [pi-cc-extensions](https://github.com/minuque/pi-cc-extensions) (MIT).

They are included here so this package can provide the `cc-light/cc-dark` theme pair without depending on `pi-cc-extensions` at runtime.
